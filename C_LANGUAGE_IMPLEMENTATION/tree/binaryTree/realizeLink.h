#include <stdio.h>

#define ERROR -1

typedef int Status;
typedef int TreeElemType;
typedef struct BinaryTreeNode{
    TreeElemType data;
    struct BinaryTreeNode *leftchild, *rightchild;
}BiTNode, *BinaryTree;

Status DLRtraverse(BinaryTree T){
    if(T){
        
    }
}